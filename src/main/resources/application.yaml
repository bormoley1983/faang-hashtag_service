spring:
  application:
    name: hashtag-service

server:
  port: ${HASHTAG_SERVICE_PORT:8084}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
